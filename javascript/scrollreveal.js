window.sr = ScrollReveal({ reset: false });

sr.reveal('.content-hero', { duration: 1000, distance: '50px', origin: 'top' });

sr.reveal('.content-card', {
  origin: 'right',       // Vem da direita
  distance: '100px',    // Distância do movimento
  duration: 1000,       // Duração da animação em ms
  delay: 150,           // Atraso antes de começar
});

sr.reveal('.text-quemsomos', {
  origin: 'left',       // Vem da esquerda
  distance: '100px',    // Distância do movimento
  duration: 1000,       // Duração da animação em ms
  delay: 150,           // Atraso antes de começar         
});

sr.reveal('.containercomofunciona', {
  origin: 'right',
  distance: '100px',
  duration: 1000,
  delay: 150,
});

sr.reveal('.container', {
  origin: 'left',
  distance: '100px',
  duration: 1000,
  delay: 150,
});

sr.reveal('.avaliacao-box', {
  origin: 'right',
  distance: '100px',
  duration: 1000,
  delay: 150,
});






